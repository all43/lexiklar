// Manifest & cache TTL
export const PHRASE_TERM_TTL_MS = 5 * 60 * 1000; // 5 minutes

// UI interactions
export const SEARCH_DEBOUNCE_MS = 300;
export const UI_YIELD_DELAY_MS = 50; // setTimeout for thread yield
export const TOAST_AUTO_DISMISS_MS = 3000;

// OTA updates
export const BUILD_MARGIN_MS = 30 * 60 * 1000; // 30 min anti-downgrade guard
